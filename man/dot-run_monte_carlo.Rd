% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.run_monte_carlo}
\alias{.run_monte_carlo}
\title{Monte Carlo Runs for One CARS Iteration (Internal)}
\usage{
.run_monte_carlo(
  X,
  y,
  current_features,
  n_select,
  max_components,
  iteration,
  N,
  cv_folds,
  random_state
)
}
\arguments{
\item{X}{Numeric matrix. Full predictor matrix (n_samples x n_features).}

\item{y}{Numeric vector. Response variable (length n_samples).}

\item{current_features}{Integer vector. Active feature indices (1-based) for
this iteration.}

\item{n_select}{Integer. Target number of features to select.}

\item{max_components}{Integer. PLS component cap (from user).}

\item{iteration}{Integer. Current CARS iteration index (used for
deterministic per-run seed offsets).}

\item{N}{Integer. Number of Monte Carlo runs to execute.}

\item{cv_folds}{Integer. Number of cross-validation folds.}

\item{random_state}{Integer. Base random seed from the \code{CARSAlgorithm} object.}
}
\value{
A named list with two elements:
\describe{
\item{\code{features}}{Integer vector of selected feature indices from the best run.}
\item{\code{rmsecv}}{Numeric. RMSECV of the best run.}
}
Returns \code{NULL} if no run succeeds.
}
\description{
Executes all \code{N} Monte Carlo sub-sampling runs for a single CARS iteration.
In each run:
\enumerate{
\item A random 80\\% calibration subset is drawn.
\item A PLS model is fitted and regression coefficients are extracted.
\item Feature indices are selected via Adaptive Reweighted Sampling (ARS).
\item The selected subset is evaluated via k-fold RMSECV on the full data.
}
Returns the feature subset and RMSECV of the best-performing run, or
\code{NULL} if every run fails.
}
\keyword{internal}
