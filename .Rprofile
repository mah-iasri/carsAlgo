source("renv/activate.R")
# Add to your project .Rprofile so it applies every time
options(
  devtools.install.args = "--no-multiarch",
  devtools.check.args   = "--no-multiarch"
)
options(renv.config.install.staged = FALSE)
Sys.setenv(RENV_CONFIG_INSTALL_ARGS = "--no-multiarch")
