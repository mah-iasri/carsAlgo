% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/carsAlgorithm.R
\name{CARSAlgorithm}
\alias{CARSAlgorithm}
\title{Competitive Adaptive Reweighted Sampling (CARS) Algorithm}
\usage{
CARSAlgorithm(max_iter = 100, N = 50, cv_folds = 5, random_state = 42)
}
\arguments{
\item{max_iter}{Maximum number of CARS iterations. Default \code{100}.}

\item{N}{Number of Monte Carlo sub-sampling runs per iteration.  Default \code{50}.}

\item{cv_folds}{Number of folds for k-fold cross-validation. Default \code{5}.}

\item{random_state}{Integer seed for reproducibility. Default \code{42}.}

\item{X}{Numeric matrix of predictors
(n_samples × n_features).}

\item{y}{Numeric response vector of length n_samples.}

\item{max_components}{Integer cap on the number of PLS latent components.
Default \code{10}.}
}
\value{
A named list with one public element:
\describe{
\item{\code{fit(X, y, max_components = 10,  save_plot= TRUE, plot_path= "./cars_rmsecv_curve.jpg")}}{Fits the CARS object on high dimentsional data matrix \code{X}
and response vector \code{y}. Returns a named list with:
\itemize{
\item \code{best_features} — sorted 1-based column indices of selected features.
\item \code{best_rmsecv}      — lowest RMSECV achieved across all iterations.
\item \code{rmsecv_history}   — numeric vector of best RMSECV per iteration.
\item \code{num_features_history} — integer vector of feature count per iteration.
}
}
}

Named list with elements \code{best_features}, \code{best_rmsecv},
\code{rmsecv_history}, and \code{num_features_history}.
}
\description{
Creates a CARS algorithm object that exposes a \verb{$fit()} method.
The constructor captures all tuning parameters in a closure; private
helper functions are nested inside and never exported.

CARS iteratively:
\enumerate{
\item Sub-samples the calibration set (Monte Carlo, \code{N} runs per iteration).
\item Fits a PLS model and extracts regression coefficients.
\item Selects variables by Adaptive Reweighted Sampling (ARS) proportional to absolute coefficient magnitude.
\item Evaluates the subset via k-fold cross-validation (RMSECV).
\item Retains the best subset and repeats with an exponentially shrinking
variable budget.
}

Fit the CARS algorithm to the high dimensional data \code{X} and response \code{y}.
}
\examples{
\dontrun{
# Simulate high dimensional data
set.seed(1)
X <- matrix(rnorm(100 * 200), nrow = 100)
y <- X[, 5] * 2 + X[, 50] * -1.5 + rnorm(100, sd = 0.5)

# Instantiate and fit
cars <- CARSAlgorithm(max_iter = 15, N = 30, cv_folds = 5, random_state = 42)
result <- cars$fit(X, y, max_components = 8)

cat("Best RMSECV      :", result$best_rmsecv, "\n")
cat("Selected features:", result$best_features, "\n")
}


}
