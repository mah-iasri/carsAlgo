% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit.CARSAlgorithm}
\alias{fit.CARSAlgorithm}
\title{Fits a CARS Object to any high dimensional dataset}
\usage{
\method{fit}{CARSAlgorithm}(
  cars_obj,
  X,
  y,
  max_components = 10L,
  plot = TRUE,
  plot_path = "../cars_rmsecv_curve.jpg",
  ...
)
}
\arguments{
\item{cars_obj}{A \code{CARSAlgorithm} object created by \code{\link{CARSAlgorithm}}.}

\item{X}{Numeric matrix of predictors (n_samples x n_features).}

\item{y}{Numeric response vector of length n_samples.}

\item{max_components}{Integer cap on PLS latent components. Default \code{10}.}

\item{plot}{Logical. Whether to display and save the RMSECV curve. Default \code{TRUE}.}

\item{plot_path}{File path for saving the RMSECV plot. Default \code{"../cars_rmsecv_curve.jpg"}.}

\item{...}{Currently unused.}
}
\value{
A named list with:
\describe{
\item{\code{best_features}}{Sorted 1-based column indices of selected features.}
\item{\code{best_rmsecv}}{Lowest RMSECV achieved across all iterations.}
\item{\code{rmsecv_history}}{Numeric vector of best RMSECV per iteration.}
\item{\code{num_features_history}}{Integer vector of feature count per iteration.}
\item{\code{plot}}{A \code{ggplot2} object of the RMSECV curve.}
}
}
\description{
Applies the CARS algorithm to a high-dimensional data matrix \code{X} and
response vector \code{y}, iteratively selecting the optimal variable subset
via Monte Carlo enabled PLS regression and adaptive reweighted sampling techniques.
}
\details{
This function iteratively:
\enumerate{
\item Sub-samples the calibration set (Monte Carlo, \code{N} runs per iteration).
\item Fits a PLS model and extracts regression coefficients.
\item Selects variables by Adaptive Reweighted Sampling (ARS) proportional
to absolute coefficient magnitude.
\item Evaluates the subset via k-fold cross-validation (RMSECV).
\item Retains the best subset and repeats with an exponentially shrinking
variable set.
}
}
\examples{
\dontrun{
set.seed(1)
X <- matrix(rnorm(100 * 200), nrow = 100)
y <- X[, 5] * 2 + X[, 50] * -1.5 + rnorm(100, sd = 0.5)

cars_obj <- CARSAlgorithm(max_iter = 15, N = 30, cv_folds = 5)
result   <- fit(cars_obj, X, y, max_components = 8)

cat("Best RMSECV      :", result$best_rmsecv, "\n")
cat("Selected features:", result$best_features, "\n")
}

}
\seealso{
\code{\link{CARSAlgorithm}}
}
